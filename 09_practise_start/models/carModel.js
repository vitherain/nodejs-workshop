const mongoose = require('mongoose');

const Schema = mongoose.Schema;

const carSchema = /* TODO: Create car schema and define its properties (brand, model, year) (cca 3 lines). TIP: use String and Number data types. */

/* TODO: Create cars model, name it 'Cars' and export it (2 lines are missing) */
